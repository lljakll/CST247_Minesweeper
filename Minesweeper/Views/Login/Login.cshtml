@{
    ViewBag.Title = "Login";
}

@*<div style="width: auto; margin: 0 auto;">
    <div class="card">
        <article class="card-body">
            <h4 class="card_title text-center mb-4 mt-1">
                Sign In
            </h4>
            <hr />
            <p class="text-success text-center">Messages Go Here Success / Fail Authentication</p>
            <form>
                <div class="form-group">
                    <div class="input-group">
                        <div class="input-group-prepend">
                            <span class="input-group-text">
                                <i class="fa fa-user">
                                    <i class="fa fa-user-ninja">
                                    </i>
                                </i>
                            </span>
                        </div>

                        <input name="" class="form-control" placeholder="Email or Login" type="email" />
                    </div>
                </div>

                <div class="form-group">
                    <div class="input-group">
                        <div class="input-group-prepend">
                            <span class="form-control">
                                <i class="fa fa-lock">
                                </i>
                            </span>
                        </div>
                        <input class="form-control" placeholder="*******" type="password" />
                    </div>
                </div>

                <div class="form-group">
                    <button type="submit" class="btn btn-primary btn-block">
                        Login
                    </button>
                </div>

                <p class="text-center">
                    <a href="#" class="btn">
                        Forgot Password?
                    </a>
                </p>
            </form>
        </article>
    </div>
</div>*@

<div class="row">
    <h2 class="text-center">Login</h2>
    <div class="col-md-6 col-md-offset-3">
        @using (Html.BeginForm("Login", "Login", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            <div class="panel">
                <div class="panel-body">
                    <fieldset class="form-group">
                        <label>Username</label>
                        @Html.TextBox("username", "", new { @class = "form-control" })
                        @Html.ValidationMessage("username")
                    </fieldset>
                    <fieldset class="form-group">
                        <label>Password</label>
                        @Html.Password("password", "", new { @class = "form-control" })
                        @Html.ValidationMessage("password")
                    </fieldset>
                    <fieldset class="form-group">
                        <button type="submit" class="btn btn-success" value="Login">Login</button>
                    </fieldset>
                </div>
            </div>
        }
    </div>
</div>